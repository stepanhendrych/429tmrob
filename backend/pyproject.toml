[project]
name = "429tmrob"
version = "0.1.0"
description = "Why participate if you are not going to win."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
